<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hewei.task.dao.TaskDao">

    <resultMap type="cn.hewei.task.entity.Task" id="TaskMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="shopId" column="shop_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="linkurl" column="linkUrl" jdbcType="VARCHAR"/>
        <result property="photourl" column="photoUrl" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="NUMERIC"/>
        <result property="couponAmount" column="coupon_amount" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="OTHER"/>
        <result property="deadline" column="deadline" jdbcType="OTHER"/>
        <result property="createdTime" column="created_time" jdbcType="TIMESTAMP"/>
        <result property="updatedTime" column="updated_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TaskMap">
        select
          id, shop_id, name, linkUrl, photoUrl, price, coupon_amount, status, deadline, created_time, updated_time
        from taobao_task.task
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TaskMap">
        select
          id, shop_id, name, linkUrl, photoUrl, price, coupon_amount, status, deadline, created_time, updated_time
        from taobao_task.task
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TaskMap">
        select
          id, shop_id, name, linkUrl, photoUrl, price, coupon_amount, status, deadline, created_time, updated_time
        from taobao_task.task
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="shopId != null">
                and shop_id = #{shopId}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="linkurl != null and linkurl != ''">
                and linkUrl = #{linkurl}
            </if>
            <if test="photourl != null and photourl != ''">
                and photoUrl = #{photourl}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="couponAmount != null">
                and coupon_amount = #{couponAmount}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="deadline != null">
                and deadline = #{deadline}
            </if>
            <if test="createdTime != null">
                and created_time = #{createdTime}
            </if>
            <if test="updatedTime != null">
                and updated_time = #{updatedTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into taobao_task.task(shop_id, name, linkUrl, photoUrl, price, coupon_amount, status, deadline, created_time, updated_time)
        values (#{shopId}, #{name}, #{linkurl}, #{photourl}, #{price}, #{couponAmount}, #{status}, #{deadline}, #{createdTime}, #{updatedTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update taobao_task.task
        <set>
            <if test="shopId != null">
                shop_id = #{shopId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="linkurl != null and linkurl != ''">
                linkUrl = #{linkurl},
            </if>
            <if test="photourl != null and photourl != ''">
                photoUrl = #{photourl},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="couponAmount != null">
                coupon_amount = #{couponAmount},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deadline != null">
                deadline = #{deadline},
            </if>
            <if test="createdTime != null">
                created_time = #{createdTime},
            </if>
            <if test="updatedTime != null">
                updated_time = #{updatedTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from taobao_task.task where id = #{id}
    </delete>

</mapper>